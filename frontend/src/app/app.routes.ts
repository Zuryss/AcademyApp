import { Routes } from '@angular/router';

import { ListaElementosComponent } from './lista-elementos/lista-elementos.component'; // Asegúrate de ajustar los nombres de los componentes

export const routes: Routes = [
  { path: 'elementos', component: ListaElementosComponent },
  // Agrega aquí más rutas según sea necesario
];
